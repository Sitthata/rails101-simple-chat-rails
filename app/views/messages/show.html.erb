<p style="color: green"><%= notice %></p>

<%= render @message %>

<div>
  <%= link_to "Edit this message", edit_message_path(@message) %> |
  <%= link_to "Back to messages", room_messages_path(@message.room) %>

  <%= button_to "Destroy this message", @message, method: :delete %>
</div>
